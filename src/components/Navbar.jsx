import React from 'react'
export default function Navbar({tab, setTab, role}){return (<nav className='nav'><button className={tab==='entrada'?'active':''} onClick={()=>setTab('entrada')}>Entrada</button><button className={tab==='saida'?'active':''} onClick={()=>setTab('saida')}>Saída</button><button className={tab==='historico'?'active':''} onClick={()=>setTab('historico')}>Histórico</button><button className={tab==='resumo'?'active':''} onClick={()=>setTab('resumo')}>Resumo Estoque</button><button className={tab==='artigos'?'active':''} onClick={()=>setTab('artigos')}>Artigos</button>{role==='administrador'&&<button className={tab==='usuarios'?'active':''} onClick={()=>setTab('usuarios')}>Usuários</button>}</nav>)}